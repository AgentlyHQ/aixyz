{
  "name": "aixyz-cli",
  "version": "0.0.0",
  "description": "CLI for building and deploying aixyz agents.",
  "license": "MIT",
  "author": "AgentlyHQ",
  "type": "module",
  "bin": {
    "aixyz-cli": "dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/bin.ts src/commands/dev-worker.ts --outdir dist --target bun",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@next/env": "^16.1.6",
    "aixyz": "workspace:*",
    "commander": "^13.0.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
