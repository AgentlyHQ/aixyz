---
title: "Quick Start"
description: "Create and run your first aixyz agent in under a minute"
---

## Prerequisites

- [Bun](https://bun.sh) 1.3.9 or later
- An OpenAI API key (for the default template)

## Create a New Agent

<Steps>
  <Step title="Scaffold the project">
    ```bash
    bunx create-aixyz-app my-agent
    cd my-agent
    ```

    The CLI prompts for an agent name and optional OpenAI API key. Use `-y` to skip prompts:

    ```bash
    bunx create-aixyz-app my-agent -y
    ```

  </Step>
  <Step title="Start the dev server">
    ```bash
    bun run dev
    ```

    Your agent starts at `http://localhost:3000` with hot reload enabled.

  </Step>
  <Step title="Test the agent">
    Verify the agent card is served:

    ```bash
    curl http://localhost:3000/.well-known/agent-card.json
    ```

  </Step>
</Steps>

## What Gets Created

```
my-agent/
  aixyz.config.ts       # Agent identity, payment, and skills
  app/
    agent.ts            # Agent definition (Vercel AI SDK)
    tools/
      weather.ts        # Example tool
    icon.png            # Agent icon
  package.json
  tsconfig.json
  vercel.json
  .env.local            # OPENAI_API_KEY goes here
```

## Endpoints

Once running, your agent exposes:

| Endpoint                       | Protocol | Description                        |
| ------------------------------ | -------- | ---------------------------------- |
| `/.well-known/agent-card.json` | A2A      | Agent discovery card               |
| `/agent`                       | A2A      | JSON-RPC task handler (x402-gated) |
| `/mcp`                         | MCP      | Tool endpoint for MCP clients      |

## Next Steps

<CardGroup cols={2}>
  <Card title="Full Setup Guide" icon="book" href="/guides/getting-started">
    Detailed walkthrough of project setup and configuration.
  </Card>
  <Card title="Agent Structure" icon="folder-tree" href="/guides/agent-structure">
    Understand config, agent, and tool files.
  </Card>
  <Card title="Deploy" icon="cloud-arrow-up" href="/guides/deployment">
    Deploy to Vercel, Docker, or standalone Bun.
  </Card>
  <Card title="Configuration" icon="gear" href="/guides/configuration">
    All config options and environment variables.
  </Card>
</CardGroup>
